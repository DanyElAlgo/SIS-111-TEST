#include <iostream>
#include <cmath>
using namespace std;

int main() {
    int base;
    int exp;
    
    cin >> base >> exp;
    
    cout << pow(base, exp);
    
}

//he tenido que hacer una investigación sobre cómo hacer potencias en lenguaje C++, y requería una nueva librería y un nuevo comando para ello
